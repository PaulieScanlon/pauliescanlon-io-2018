import styled from "react-emotion";
import { common } from "../../styles";

export const PortfolioWrapper = styled.div({
  label: "portfolio-wrapper",
  display: "flex",
  flexDirection: "column",
  marginBottom: common.flexBottom
});
