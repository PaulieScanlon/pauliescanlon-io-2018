import styled from "react-emotion";

import { common } from "../../styles/";

export const ArrayGridWrapper = styled.div({
  label: "array-grid-wrapper",
  display: "flex",
  flexWrap: "wrap",
  margin: `0 -${common.padding / 2}px 0 -${common.padding / 2}px`
});
